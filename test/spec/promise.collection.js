'use strict';

describe('Promise helper', function () {
	var Promise = require('../../src/promise.js')
	
	var promise1
	var values1

	beforeEach(function(){
		
		values1 = [1,2, null, undefined, new Error(), 3]

	})

	describe('all', function () {
		//Promise.some(A).then(function(R) {
			//	console.log(R)
			//	console.log(R.filter(function() { return true}))
			//})
		it('', function(done){
			
		})
	})

	describe('some', function () {
		it('', function(done){
			
		})
	})

	describe('any', function () {
		it('', function(done){
			
		})
	})
})